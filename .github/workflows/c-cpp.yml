name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: ilammy/setup-nasm@v1.5.2
    - uses: actions/checkout@v4
    - name: make
      run: make
    - name: make run
      run: make run
